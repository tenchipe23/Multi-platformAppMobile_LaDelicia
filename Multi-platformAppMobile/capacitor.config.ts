import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Multi-platformAppMobile',
  webDir: 'www'
};

export default config;
